# Define the browsers your project supports
# Autoprefixer uses this to determine which vendor prefixes to add

# Target the last 2 major versions of all browsers
> 1%
last 2 versions

# Explicitly include specific browsers for better compatibility
Chrome >= 90
Firefox >= 88
Safari >= 14
Edge >= 90
iOS >= 14
Android >= 10
